version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static support dashboard..."
        - ls -la
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []

version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static support dashboard..."
        - echo "No build process needed for static HTML/CSS/JS files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
